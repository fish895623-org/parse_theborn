name: main

on:
  push:
    branches: [ master, develop ]
  pull_request:
    branches: [ master, develop ]

jobs:

  build:

    runs-on: instance

    steps:
    - uses: actions/checkout@v2
    - name: testing
      run: echo test
